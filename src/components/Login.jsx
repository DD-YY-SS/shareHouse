import { React, useState, ArrowRight, API, accounts } from '../shared.js';
export default function Login({onLogin}){const[id,setId]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const submit=async e=>{e.preventDefault();try{const response=await fetch(API+'/api/v1/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({accountId:id,password})});const data=await response.json();if(!response.ok)throw Error();onLogin(data)}catch{setError('Login failed. Check the API server and credentials.')}};return <div className="app-shell login-shell"><section className="login-page"><div className="login-brand"><img className="app-logo login-logo" src="/checkmate-logo.svg" alt="CheckMate logo"/><strong>CheckMate</strong></div><div className="login-copy"><div className="eyebrow"><span className="live-dot"/>OPERATOR-POWERED MATCHING</div><h1>Safe living starts<br/>with the right match.</h1><p>Verify identity, answer behavior questions, and meet one roommate safely.</p></div><form className="login-card" onSubmit={submit}><div className="login-card-head"><strong>Log in</strong><span>Demo account</span></div><label>Account ID<input value={id} onChange={e=>setId(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="login-error">{error}</p>}<button className="primary-button">Log in <ArrowRight size={18}/></button></form><div className="quick-accounts"><p>Quick fill</p><div>{accounts.slice(0,2).map(([label,value])=><button type="button" key={value} onClick={()=>{setId(value);setPassword('1234')}}><span>{label}</span><small>{value} - 1234 auto-filled</small></button>)}</div></div></section></div>}
