import { React, useState, ArrowRight, API, accounts } from '../shared.js';
export default function Login({onLogin,variantContext,onChangeVariant}){const[id,setId]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const submit=async e=>{e.preventDefault();try{const response=await fetch(API+'/api/v1/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({accountId:id,password,variant:variantContext.id})});const data=await response.json();if(!response.ok)throw Error();onLogin(data)}catch{setError('로그인에 실패했어요. 아이디와 비밀번호를 확인해 주세요.')}};return <div className="app-shell login-shell"><section className="login-page"><div className="login-brand"><img className="app-logo login-logo" src="/checkmate-logo.svg" alt="CheckMate 로고"/><strong>CheckMate</strong></div><button type="button" className="login-variant-pill" onClick={onChangeVariant}>선택 버전: {variantContext.id==='standard'?'현재 버전':variantContext.id==='dorm'?'대학교 기숙사':'거리 기반 1km'} 변경</button><div className="login-copy"><div className="eyebrow"><span className="live-dot"/>안심 매칭 서비스</div><h1>안전한 생활은<br/>잘 맞는 룸메이트에서 시작돼요.</h1><p>행동 기반 설문을 바탕으로 한 명의 룸메이트를 안전하게 찾아요.</p></div><form className="login-card" onSubmit={submit}><div className="login-card-head"><strong>로그인</strong><span>테스트 계정</span></div><label>아이디<input value={id} onChange={e=>setId(e.target.value)} autoComplete="username"/></label><label>비밀번호<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/></label>{error&&<p className="login-error">{error}</p>}<button className="primary-button">로그인하기 <ArrowRight size={18}/></button></form><div className="quick-accounts"><p>테스트 계정 빠른 입력</p><div>{accounts.slice(0,2).map(([label,value])=><button type="button" key={value} onClick={()=>{setId(value);setPassword('1234')}}><span>{label==='Tenant 1'?'세입자 1':'세입자 2'}</span><small>{value} · 비밀번호 1234 자동 입력</small></button>)}</div></div></section></div>}
