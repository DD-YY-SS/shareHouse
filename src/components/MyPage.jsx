import { React, BadgeCheck, ChevronLeft, LogOut, UserRound } from '../shared.js';
export default function MyPage({auth,back,logout}){return <section className="page-pad inner-page my-page"><button className="back-button" onClick={back}><ChevronLeft size={19}/>Back</button><div className="section-kicker">MY CHECKMATE</div><h2>My profile<br/>and safety settings</h2><div className="my-profile-card"><div className="my-profile-avatar"><UserRound size={31}/></div><div><strong>{auth.user.pseudonym||'Tenant'}</strong><span>Tenant account</span></div><BadgeCheck size={22}/></div><div className="my-account-note"><span>Account ID</span><strong>{auth.user.accountId}</strong></div><button className="logout-button" onClick={logout}><LogOut size={18}/>Log out</button></section>}
