import { React, useEffect, useState, BadgeCheck, ArrowRight, Check, Clock3, FileCheck2, MessageCircle, ShieldCheck } from '../shared.js';
import MediationSheet from './MediationSheet.jsx';
export default function CarePage({roomId,back}){const[start]=useState(()=>sessionStorage.getItem('cm-care-start')||new Date().toISOString()),[now,setNow]=useState(Date.now()),[rules,setRules]=useState(false),[sos,setSos]=useState(false),[notice,setNotice]=useState('');useEffect(()=>{sessionStorage.setItem('cm-care-start',start);const timer=setInterval(()=>setNow(Date.now()),60000);return()=>clearInterval(timer)},[start]);const elapsed=Math.min(30,Math.max(0,Math.floor((now-Date.parse(start))/86400000))),left=Math.max(0,30-elapsed),progress=Math.round(elapsed/30*100);return <section className="page-pad inner-page care-page"><div className="care-header"><div><div className="care-page-badge"><ShieldCheck size={17}/>30-DAY CARE</div><h2>Reservation confirmed.<br/>Care has started.</h2><p className="muted">Room {roomId||'101'} - safer adaptation together.</p></div><div className="care-celebrate"><Check size={24}/></div></div><div className="care-dday-card"><div className="care-dday-row"><div><span>Care progress</span><strong>Day {Math.min(30,elapsed+1)}</strong></div><b>{left? 'D-'+left:'Complete'}</b></div><div className="care-progress"><i style={{width:progress+'%'}}/></div><p>We support living adaptation and conflict mediation for 30 days.</p></div><div className="care-timeline"><div className="care-timeline-item active"><span>1</span><div><strong>Move-in and agreement complete</strong><p>Rules and match conditions are saved.</p></div><Check size={17}/></div><div className="care-timeline-item active"><span>2</span><div><strong>30-day care period</strong><p>{left?'Care is currently active.':'Care period complete.'}</p></div><Clock3 size={17}/></div><div className="care-timeline-item"><span>3</span><div><strong>Final satisfaction check-in</strong><p>We will ask about satisfaction and conflicts.</p></div><BadgeCheck size={17}/></div></div><div className="care-section-title"><span>QUICK ACTIONS</span><small>Open help when you need it</small></div><div className="care-action-grid"><button className="care-action-card" onClick={()=>setRules(true)}><span className="care-action-icon purple"><FileCheck2 size={21}/></span><strong>View living agreement</strong><small>Three shared rules</small><ArrowRight size={16}/></button><button className="care-action-card" onClick={()=>setSos(true)}><span className="care-action-icon red"><ShieldCheck size={21}/></span><strong>Request mediation</strong><small>You do not have to solve it alone</small><ArrowRight size={16}/></button></div>{notice&&<p className="care-notice"><ShieldCheck size={14}/>{notice}</p>}<div className="care-support-card"><MessageCircle size={21}/><div><strong>You do not have to solve it alone.</strong><p>Choose an objective reason and the operator will contact you privately.</p></div></div>{rules&&<div className="care-overlay" onClick={()=>setRules(false)}><div className="care-modal" onClick={e=>e.stopPropagation()}><div className="care-modal-head"><div><span className="section-kicker">QUICK RULEBOOK</span><h3>Our living agreement</h3></div><button onClick={()=>setRules(false)}>Close</button></div><div className="care-rule"><Check size={17}/><div><strong>Quiet hours</strong><span>Keep shared areas quiet after 23:00.</span></div></div><div className="care-rule"><Check size={17}/><div><strong>Cleaning</strong><span>Clean shared areas at least weekly.</span></div></div><div className="care-rule"><Check size={17}/><div><strong>Guests</strong><span>Notify each other before visits.</span></div></div></div></div>}{sos&&<MediationSheet roomId={roomId} close={()=>setSos(false)} onDone={message=>{setNotice(message);setSos(false)}}/>}</section>}
