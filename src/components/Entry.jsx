import { React, ArrowRight, Check, FileCheck2, ShieldCheck } from '../shared.js';
import HomeLounge from './HomeLounge.jsx';
export default function Entry({context,consent,setConsent,next,profileComplete,openMatching,resident,auth,matchId,candidateId}){if(profileComplete)return <HomeLounge context={context} openMatching={openMatching} resident={resident} auth={auth} matchId={matchId} candidateId={candidateId}/>;return <section className="page-pad entry-page"><div className="operator-line"><span className="operator-logo">O</span><span><strong>Operator co-living house</strong><small>Embedded flow - room {context.roomId}</small></span></div><div className="section-kicker">CHECKMATE EMBEDDED</div><h1>Safer move-in<br/><em>with CheckMate</em></h1><p className="lead">Verify identity and affiliation, then find a roommate through transparent behavior rules.</p><div className="entry-visual"><ShieldCheck size={44}/><div><strong>Operator-powered safety matching</strong><span>Identity, affiliation, and behavior data</span></div></div><div className="consent-card"><div className="consent-title"><FileCheck2 size={19}/><strong>Consent and verification</strong></div><p>We use identity verification and affiliation email verification within the legally permitted scope.</p><label className="consent-check"><input type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)}/><span><Check size={13}/></span><b>I consent to the submission-based safety information check.</b></label><small>Original sensitive data is not shared. Only verification results are used.</small></div><button className="primary-button large" disabled={!consent} onClick={next}>Start behavior profile <ArrowRight size={18}/></button></section>}
